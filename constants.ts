export const LANE_WIDTH_PCT = 33.33;
export const PLAYER_Y_POS = 80; // Player is at 80% down the screen
export const INITIAL_SPEED = 0.6; // Speed increment per frame
export const MAX_SPEED = 2.5;
export const SPEED_INCREMENT = 0.05; // Speed boost per coffee
export const SPAWN_RATE_BASE = 60; // Frames between spawns (decreases as speed increases)

export const COLORS = {
  coffee: '#d97706', // amber-600
  milk: '#f5f5f4',   // stone-100
  accent: '#78350f', // amber-900
};
